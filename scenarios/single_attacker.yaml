# TODO: extend scenario metadata (experiment id, train/eval split) before running large sweeps.
world:
  name: Shanghai_map
agents:
  - id: A
    vehicle:
      name: racecar
      sensors: [ lidar, pose, collision  ]
    task:
      task_name: gap_follow
      params: {laps: 1, time_limit: 120.0, terminate_on_collision: True}
  - id: B
    vehicle:
      name: racecar
      sensors: [ lidar, pose, collision ]
    task:
      task_name: gap_follow
      params: {laps: 1, time_limit: 120.0, terminate_on_collision: True}
  
