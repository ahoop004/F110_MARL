# Matched individual-reward comparison under the current driving contract.
includes:
  - mappo_2v2_completion.yaml
experiment:
  name: mappo_2v2_individual
training_defaults:
  team_return_mode: per_agent
mappo:
  reward_mode: individual
  critic_mode: agent_conditioned
wandb:
  group: mappo-2v2-individual
