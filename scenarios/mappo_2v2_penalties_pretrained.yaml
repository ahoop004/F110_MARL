# PPO actor initialization only; centralized critic and optimizer start fresh.
includes:
  - ../configs/scenarios/mappo_2v2_penalties_base.yaml
experiment:
  name: mappo_2v2_penalties_pretrained
training_defaults:
  # Produced by the current pretraining run; override with --pretrained-actor PATH.
  # A missing checkpoint fails instead of silently running the scratch arm.
  pretrained_actor_checkpoint: ../outputs/L_map_pretrain/L_map_best_model.pt
