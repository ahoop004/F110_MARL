includes:
  - ../configs/env/default.yaml
  - ../configs/vehicle/default.yaml

experiment:
  name: nrl_1car
  episodes: 45   # probe.py distributes across maps (5 per map × 9 maps)
  seed: 42

environment:
  maps: auto           # auto-discovers all maps with centerline + walls CSVs
  map_cycle: per_episode
  map_pick: round_robin
  max_steps: 30000
  action_repeat: 2
  centerline_autoload: true
  walls_autoload: true
  random_spawn:
    enabled: true
    allow_reuse: true

agents:
  car_0:
    algorithm: pure_pursuit
    params:
      lookahead: 2.0
      min_speed: 0.5
      max_speed: 1.5
      max_steer: 0.42
      wheelbase: 0.33020
      curvature_slowdown_threshold: 0.15
      speed_horizon: 5.0
      blend_distance: 1.5
      full_ftg_distance: 0.8
      ftg:
        max_distance: 10.0
        bubble_radius: 2.5
        steering_gain: 0.30
        steer_smooth: 0.5
        use_disparity_extender: true
        disparity_threshold: 0.35
        vehicle_width: 0.225
        safety_margin: 0.10


wandb:
  enabled: false
