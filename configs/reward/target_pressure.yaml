reward:
  target_proximity:
    enabled: true
    weight: 1.0
    preferred_distance: 1.5
    distance_tolerance: 0.5

  target_edge_pressure: {enabled: true, weight: 0.5}

  collision: {enabled: true, penalty: -200.0}

  target_crash_bonus: {enabled: true, bonus: 200.0}

  timeout_penalty: {enabled: true, penalty: -100.0}

  self_crash_penalty: {enabled: true, penalty: -20.0}
