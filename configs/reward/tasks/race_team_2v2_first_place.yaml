includes:
  - race_team_completion.yaml

task:
  name: race_team_2v2_first_place
  description: Mean completion shaping plus a shared bonus when a teammate finishes first.

reward:
  team_race_result:
    enabled: true
    objective: first_place
    win_bonus: 2.0
