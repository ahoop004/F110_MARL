agents:
  car_3:
    role: attacker
    algorithm: hybrid_pp_ftg
    params:
      lookahead: 1.5
      min_speed: 0.5
      max_speed: 3.0
      max_steer: 0.42
      wheelbase: 0.33020
      curvature_slowdown_threshold: 0.3
      speed_horizon: 3.0
      blend_distance: 3.0         # LiDAR (m) at which FTG starts blending in
      full_ftg_distance: 1.5      # LiDAR (m) at which FTG has full authority
      ftg:                        # FTG params used when obstacle is detected
        max_distance: 10.0
        window_size: 4
        bubble_radius: 2.5
        max_steer: 0.42
        min_speed: 0.5
        max_speed: 3.0
        steering_gain: 0.30
        fov: 4.71238898
        normalized: false
        steer_smooth: 0.5
        mode: lidar
        gap_min_range: 0.5
        target_mode: center
        wall_avoid_kick: 0.02
        use_disparity_extender: true
        disparity_threshold: 0.40
        vehicle_width: 0.225
        safety_margin: 0.10
