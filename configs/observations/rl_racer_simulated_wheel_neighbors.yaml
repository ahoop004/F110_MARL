# Preserve the 50 driving inputs, then append three relative neighbor slots.
includes:
  - rl_racer_simulated_wheel.yaml
observation:
  frenet_neighbors:
    enabled: true
    max_neighbors: 3
    maxima: {delta_s: 30.0, delta_d: 5.0, delta_vs: 20.0, delta_vd: 20.0}
    clip: false
